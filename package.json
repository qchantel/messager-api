{
  "name": "api",
  "version": "1.0.0",
  "description": "- `doppler login` - `doppler setup -p notice-v2 -c dev_api` - might be optional because we also added a .yaml - `doppler secrets download --no-file --format=env > .env` - you'll have to preprend each var with REACT_APP_",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.mjs",
    "start": "node index.mjs"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "chalk": "^5.3.0",
    "dotenv": "^16.4.5",
    "express.js": "^1.0.0",
    "fluent-ffmpeg": "^2.1.3",
    "mongodb": "^6.6.2",
    "node-telegram-bot-api": "^0.65.1",
    "openai": "^4.47.1"
  }
}
